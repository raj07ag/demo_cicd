name: demo ci-cd pipeline

on:
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t iris-model:latest

      - name: Run Container Tests
        run: docker run iris-model:latest pytest tests/